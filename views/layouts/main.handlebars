<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootswatch/4.1.3/flatly/bootstrap.min.css" />
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <header class="container">
    <div class="row flex-nowrap justify-content-between align-items-center">
      <div class="text-center">
        {{#unless logged_in}}
        <p class="display-4 font-italic text-dark">Welcome to the Tech Blog!</p>
        {{/unless}}  
      </div>

      <div class="col-4 d-flex justify-content-end align-items-center m-4">
        {{#if logged_in}}
        {{!-- Need a route for the href here that will then render the handlebar being used --}}
        <a class="btn btn-sm btn-primary" href="/dashboard/createpost/">CREATE POST</a>
        <a class="btn btn-sm ml-4" href="/dashboard">Back to Dashboard</a>
        <a class="btn btn-sm m-4" href="/">HOME</a>
        <a class="btn btn-sm " id = "logout" href="/js/logout.js">Log Out</a>
        {{else}}
        <a class="btn btn-sm btn-primary m-2" href="/login">Login</a>
        <a class="btn btn-sm btn-danger m-2" href="/signup">Sign-Up</a>
        <a class="btn btn-sm btn-outline-secondary m-2" href="/">HOME</a>
        {{/if}}
      </div>
    </div>
  </header>

  <main>
   {{{body}}}
  </main>
    
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>